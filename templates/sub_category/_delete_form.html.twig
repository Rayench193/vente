<form onsubmit="return confirm('voulez vous vraiment supprimer cette sous catégorie ? ')" method="post" action="{{ path('app_sub_category_delete', {'id': sub_category.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ sub_category.id) }}">
    <button class="btn btn-danger col-2 m-lg-1">
        Delete <i class="fa-solid fa-trash" style="font-size:15px ; margin:5px"></i>
    </button>
</form>
